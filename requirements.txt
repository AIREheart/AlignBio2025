# Core dependencies
torch>=2.0.0
torch-geometric>=2.3.0
torch-scatter>=2.1.0
torch-sparse>=0.6.17

# Data handling
pandas>=2.0.0
numpy>=1.24.0
openpyxl>=3.1.0
scikit-learn>=1.3.0
scipy>=1.11.0

# Protein ML
fair-esm>=2.0.0  # For ESM2/ESM3 embeddings
biopython>=1.81  # For PDB parsing

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0
networkx>=3.1

# Optional but recommended
jupyter>=1.0.0
tensorboard>=2.13.0
tqdm>=4.65.0

# For PyTorch Geometric dependencies, also run:
# pip install pyg_lib torch_scatter torch_sparse torch_cluster torch_spline_conv -f https://data.pyg.org/whl/torch-${TORCH}+${CUDA}.html
# where ${TORCH} is your PyTorch version (e.g., 2.0.0) and ${CUDA} is your CUDA version (e.g., cu118)
